diff --git a/packages/core/installer/hack/gen-profiles.sh b/packages/core/installer/hack/gen-profiles.sh
index 72f23ee..new456 100755
--- a/packages/core/installer/hack/gen-profiles.sh
+++ b/packages/core/installer/hack/gen-profiles.sh
@@ -30,9 +30,6 @@ for extension in $EXTENSIONS; do
   export "$extension_var=$version"
 done
 
-SPIN_VERSION="v0.19.0"
-TAILSCALE_VERSION="1.84.0"
-
 for profile in $PROFILES; do
   echo "writing profile images/talos/profiles/$profile.yaml"
   case "$profile" in
@@ -62,13 +59,13 @@ for profile in $PROFILES; do
   cat > images/talos/profiles/$profile.yaml <<EOT
 # this file generated by hack/gen-profiles.sh
 # do not edit it
-arch: amd64
+arch: arm64
 platform: ${platform}
 secureboot: false
 version: ${TALOS_VERSION}
 input:
   kernel:
-    path: /usr/install/amd64/vmlinuz
+    path: /usr/install/arm64/vmlinuz
   initramfs:
-    path: /usr/install/amd64/initramfs.xz
+    path: /usr/install/arm64/initramfs.xz
   baseInstaller:
     imageRef: "ghcr.io/siderolabs/installer:v1.10.3"
   systemExtensions: